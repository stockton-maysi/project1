/**
 * An ItemSlot specifically for Equipment
 */
public class EquipmentSlot extends ItemSlot {
	/**
	 * Constructs a new EquipmentSlot
	 * @param s The slot name, determining which items can go inside it
	 */
	public EquipmentSlot(String s) {
		super(s);
	}
}